# Generated Types

`database.types.ts` is generated from the Supabase schema and should not be handwritten.

Generate it after local Supabase is running:

```bash
supabase gen types typescript --local > src/types/database.types.ts
```

